name: flutter_config_plugin
description: A Flutter config plugin system inspired by expo/config-plugins. Programmatically modify iOS and Android native project files through a composable plugin pipeline.
version: 0.0.1

environment:
  sdk: ^3.10.7

dependencies:
  xml: ^6.0.0
  path: ^1.8.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
